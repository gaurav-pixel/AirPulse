# AirPulse Frontend

React + Vite frontend for the AirPulse air quality platform.

## Setup

```bash
npm install
npm run dev
```

The dev server runs on `http://localhost:5173` and proxies API calls to the FastAPI backend on port 8000.
